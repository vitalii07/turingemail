#############################
# These are security tests. #
#############################

describe "TuringEmailApp", ->

  it "should detect and stop dictionary attacks on emails", ->
    return

  it "should fully log out a user", ->
    return

  it "should require two-factor authentication for login when configured to do so", ->
    return
